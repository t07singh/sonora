1) Deploy backend (FastAPI) to a Python host (Heroku/Fly/Railway/Render).
   - Set env vars: OPENAI_API_KEY, ELEVENLABS_API_KEY, GPT_MODEL, DEFAULT_VOICE, USE_LOCAL_WHISPER

2) Deploy frontend (static) to Vercel/Netlify/S3+Cloudfront or any nginx.
   - If backend is not localhost, add in the HTML head: 
     <script>window.BACKEND_BASE = 'https://<BACKEND_HOST>';</script>

3) Your landing page can embed the editor as:
   <iframe src="https://<FRONTEND_HOST>" width="100%" height="800"></iframe>

4) YC application: share both the live site link and a direct API pipeline endpoint, plus a short audio file link for testing.







