# Sonora Segmenter Service — Requirements
# Anime-first video segmentation pipeline

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6
httpx>=0.25.0

# ASR — Whisper transcription
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# VAD — Voice Activity Detection
torch>=2.0.0
torchaudio>=2.0.0

# Diarization — Speaker identification
pyannote.audio>=3.1

# Japanese text processing
fugashi>=1.3.0
unidic-lite>=1.0.8

# Video/Audio processing
ffmpeg-python>=0.2.0
soundfile>=0.12.0

# Forced alignment — Qwen3 (PRIMARY, SOTA)
transformers>=4.40.0
accelerate>=0.25.0
sentencepiece>=0.1.99

# Forced alignment — wav2vec2 (LEGACY FALLBACK)
# Already covered by transformers + torch + torchaudio above

# Vocal isolation (optional)
demucs>=4.0.0

# Redis for distributed locking
redis>=5.0.0

# Utilities
psutil>=5.9.0
python-dotenv>=1.0.0
